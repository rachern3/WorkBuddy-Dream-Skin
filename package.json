{
  "name": "workbuddy-dream-skin",
  "version": "0.1.0",
  "private": true,
  "description": "External theme and wallpaper engine for the official WorkBuddy desktop app",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "bash tests/run-tests.sh"
  },
  "engines": {
    "node": ">=22"
  }
}
